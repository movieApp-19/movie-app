import { removeUserFromGroup } from "../models/fanGroupModel";

const deleteUserFromGroup = async (req, res, next) => {
    try{

    } catch(error) {
        return next(error);
    }
}

export { deleteUserFromGroup };